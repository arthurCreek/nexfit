import React from 'react';
import Layout from '../components/Layout';

const index = () => (
    <Layout>
        <div>
            <h1>Home Page</h1>
        </div>
    </Layout>
);

export default index;